# RiskShield-ElasticNet
An enterprise credit risk pipeline built to predict loan defaults. It implements SMOTE to handle heavily imbalanced financial ledger datasets, trains an ElasticNet classification model, and uses ROC-AUC optimization to maximize bank profit.
